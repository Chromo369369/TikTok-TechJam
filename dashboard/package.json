{
  "name": "techjam-visualization-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "data": "node scripts/build-dashboard-data.mjs",
    "dev": "vite",
    "build": "npm run data && tsc -b && vite build",
    "typecheck": "tsc -b"
  },
  "dependencies": { "@vitejs/plugin-react": "latest", "vite": "latest", "typescript": "latest", "react": "latest", "react-dom": "latest" },
  "devDependencies": { "@types/react": "latest", "@types/react-dom": "latest" }
}
